@{
    ViewData["Title"] = "Home Page";
}
@{
    ViewData["Title"] = "Habitly";
}
@{
    Layout = null;
}


@{
    ViewData["Title"] = "Habitly";
    Layout = null;
}

<!-- Importa a fonte Inter (mantém suas cores) -->
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;900&display=swap" rel="stylesheet">

<style>
    :root {
        --primary: #007A80;
        --text: #2b2f33;
        --muted: #495057;
        --bg: #ffffff;
        --soft: #f5f7f7;
    }

    /* ====== Layout base ====== */
    .page {
        min-height: 100vh;
        background: var(--bg);
        position: relative;
        overflow-x: hidden;
        font-family: 'Inter', system-ui, -apple-system, sans-serif; /* 👈 trocado aqui */
    }

    /* ====== Header ====== */
    .site-header {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        padding: 20px;
        display: flex;
        align-items: center;
        align-items: center;
        z-index: 1000;
    }

    .brand {
        font-size: clamp(32px, 6vw, 72px);
        font-weight: 900;
        color: var(--primary);
        margin: 0;
        letter-spacing: .5px;
        font-family: 'Inter', sans-serif;
    }

    /* ====== Demais estilos originais ====== */
    /* ===== Logo ===== */
    .brand-logo {
        height: 200px;
        width: auto;
        display: block;
        margin: 20px auto 0 auto; /* espaçamento superior e centralização */
        filter: drop-shadow(0 3px 6px rgba(0,0,0,0.1));
        transition: transform 0.3s ease;
    }

        .brand-logo:hover {
            transform: scale(1.05);
        }

    /* ===== Header ===== */
    .site-header {
        position: relative; /* antes era absolute — isso evita sobreposição */
        padding: 20px 0 5px 0; /* adiciona espaço acima e abaixo do logo */
        text-align: center;
        background-color: var(--bg);
        z-index: 1000;
    }

    /* ===== Hero Section ===== */
    .hero {
        padding-top: 20px; /* adiciona espaçamento extra abaixo do header */
        padding-bottom: 64px;
        text-align: center;
        background: radial-gradient(1200px 500px at 80% -20%, rgba(0,122,128,.10), transparent 60%), radial-gradient(1200px 500px at 20% -30%, rgba(0,122,128,.06), transparent 60%), var(--bg);
    }

    .cta {
        display: flex;
        gap: 12px;
        justify-content: center;
        flex-wrap: wrap;
    }

    .btn--cta {
        padding: 14px 24px !important;
        font-size: 18px !important;
        border-radius: 12px !important;
    }

    .features {
        padding: 36px 20px 80px;
        background: var(--soft);
    }

    .features-grid {
        max-width: 1100px;
        margin: 0 auto;
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 20px;
    }

    .card {
        background: #fff;
        border: 1px solid rgba(0,0,0,.06);
        border-radius: 16px;
        padding: 24px;
        box-shadow: 0 8px 30px rgba(0,0,0,.05);
        text-align: left;
    }

        .card h3 {
            margin: 0 0 8px;
            font-size: 20px;
            color: var(--text);
        }

        .card p {
            margin: 0;
            color: var(--muted);
            font-size: 16px;
            line-height: 1.5;
        }

    .site-footer {
        text-align: center;
        padding: 32px 16px 48px;
        color: #6b7176;
        font-size: 14px;
        background: #fff;
    }
    /* ====== Botões principais ====== */
    .btn {
        display: inline-block;
        padding: 14px 28px;
        border-radius: 12px;
        font-size: 18px;
        font-weight: 600;
        text-decoration: none;
        cursor: pointer;
        transition: all 0.25s ease;
        letter-spacing: 0.3px;
    }

    /* Botão sólido (verde) */
    .btn--cadastro {
        background-color: var(--primary);
        color: #fff;
        border: 2px solid var(--primary);
    }

        .btn--cadastro:hover {
            background-color: #009093; /* tom ligeiramente mais escuro */
            border-color: #009093;
            transform: translateY(-1px);
            box-shadow: 0 4px 10px rgba(0, 122, 128, 0.25);
        }

    /* Botão contornado (transparente) */
    .btn--login {
        background-color: transparent;
        color: var(--primary);
        border: 2px solid var(--primary);
    }

        .btn--login:hover {
            background-color: var(--primary);
            color: #fff;
            transform: translateY(-1px);
            box-shadow: 0 4px 10px rgba(0, 122, 128, 0.25);
        }

    /* Layout da área de botões */
    .cta {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        align-items: center;
        gap: 16px;
        margin-top: 32px;
    }
    

</style>


<div class="page">
    <!-- Header -->
    <header class="site-header" role="banner" aria-label="Topo do site">
        <img src="~/img/habitly_img.png" alt="Habitly logo" class="brand-logo" />

    </header>
    <!-- Hero -->
    <section class="hero">
        <h1>Construa hábitos fortes.</h1>
        <h1>Dia após dia.</h1>
        <p>
            Quer ler mais, treinar com constância ou aprender um idioma?
        </p>
        <p>
            O Habitly ajuda você a <strong>definir metas</strong>, acompanhar o
            <strong>progresso</strong> e manter a <strong>motivação</strong>.
        </p>
        <p>
            O futuro é a soma dos seus hábitos.
        </p>
        <div class="cta">
            @if (User.Identity?.IsAuthenticated ?? false)
            {
                <a class="btn btn--cadastro btn--cta" asp-controller="Habitos" asp-action="Index">Ir para meus hábitos</a>
            }
            else
            {
                <a class="btn btn--cadastro btn--cta" asp-controller="Usuarios" asp-action="Create">Começar agora</a>
                <a class="btn btn--login btn--cta" asp-controller="Usuarios" asp-action="Login">Já tenho conta</a>
            }
        </div>
    </section>

    <!-- Features -->
    <section class="features" aria-label="Recursos do Habitly">
        <div class="features-grid">
            <article class="card">
                <h3>Metas claras</h3>
                <p>Crie hábitos com periodicidade, alarmes e metas semanais. Simples de configurar, fácil de manter.</p>
            </article>
            <article class="card">
                <h3>Progresso visível</h3>
                <p>Checklist diário e métricas como streak, taxa de cumprimento e melhores sequências.</p>
            </article>
            <article class="card">
                <h3>Motivação contínua</h3>
                <p>Notificações, recompensas e mensagens de incentivo para manter a constância ao longo do tempo.</p>
            </article>
        </div>
    </section>

    <!-- Footer -->
    <footer class="site-footer" role="contentinfo">
        © @DateTime.Now.Year Habitly — Construa hoje o seu amanhã.
    </footer>
</div>

