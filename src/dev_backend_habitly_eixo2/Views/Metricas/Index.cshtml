@model IEnumerable<dev_backend_habitly_eixo2.Models.Metrica>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdUsuario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StreakAtual)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StreakMaximo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdUsuario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StreakAtual)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StreakMaximo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdMetrica">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdMetrica">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdMetrica">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
